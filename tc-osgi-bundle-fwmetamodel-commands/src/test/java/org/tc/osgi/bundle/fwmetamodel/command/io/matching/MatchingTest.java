package org.tc.osgi.bundle.fwmetamodel.command.io.matching;

import org.junit.Test;

/**
 * MatchingTest.java.
 * @author Collonville Thomas
 * @version 0.0.1
 *  @req STD_BUNDLE_FWMETAMODEL_COMMANDS_020
 * @tack SRS_BUNDLE_FWMETAMODEL_COMMANDS_010, SRS_BUNDLE_FWMETAMODEL_COMMANDS_020, SRS_BUNDLE_FWMETAMODEL_COMMANDS_030
 */
public class MatchingTest {

    /**
     * test.
     */
    @Test
    public void test() {
        // try {
        // new LoadMetaModel("MetaModelReseauRoutier.xml");
        // } catch (ClassNotFoundException e) {
        // fail(e.getMessage());
        // } catch (IOException e) {
        // fail(e.getMessage());
        // }

        // new PrintMetaModelOnConsole("ReseauRoutier");

        // Iterator it = Command.getRepository().getCommandsIterator();
        // while (it.hasNext()) {
        // System.out.println(it.next().toString());
        // }
    }

}
